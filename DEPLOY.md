# Claude 앱 커스텀 커넥터로 올리기

내 PC에서만 쓰던 MCP를 인터넷에 올려, **Claude 앱(웹·모바일 포함)에서 커넥터로 연결**하는 방법입니다.

전체 30~40분, 비용은 무료로 시작할 수 있습니다.

```
내 PC 파일  →  ① GitHub 저장소  →  ② Render 배포  →  ③ Claude 커넥터 등록
```

> **왜 인터넷에 올려야 하나요?**
> 지금 방식(stdio)은 내 컴퓨터에서 Claude 데스크톱 앱이 직접 실행하는 구조라, 그 PC에서만 동작합니다.
> 커스텀 커넥터로 올리면 휴대폰에서도, 다른 사람도 쓸 수 있습니다.

---

## 준비 — 올릴 파일 확인

이 파일들이 필요합니다.

| 파일 | 역할 |
|---|---|
| `server.py` | MCP 서버 본체 |
| `sources.py` | 경주·대구 데이터 수집 |
| `requirements.txt` | 필요한 프로그램 목록 |
| `render.yaml` | 배포 설정 |
| `Dockerfile` | 다른 호스팅을 쓸 때만 필요 |
| `.gitignore` | 올리지 않을 파일 목록 |
| `README.md` | 설명서 |

`selftest.py`, `probe_*.py`는 진단용이라 올려도 되고 안 올려도 됩니다.

---

## ① GitHub에 올리기

### 1-1. 저장소 만들기

1. https://github.com 로그인
2. 오른쪽 위 **+** → **New repository**
3. 입력
   - Repository name: `dongne-pyeongsaeng`
   - **Public** 선택 (Private도 되지만 Render 연결이 한 단계 늘어납니다)
   - 나머지는 그대로 두고 **Create repository**

### 1-2. 파일 올리기

만들어진 화면에서 **uploading an existing file** 링크를 누릅니다.

1. 위 표의 파일들을 창으로 끌어다 놓기
2. 아래 `Commit changes` 버튼 클릭

> 명령어(git)를 몰라도 됩니다. 웹에서 끌어다 놓는 것으로 충분합니다.
> 나중에 파일을 고칠 때도 같은 방법으로 덮어쓰면 됩니다.

---

## ② Render에 배포하기

### 2-1. 가입

https://render.com → **Get Started** → **GitHub로 로그인**

### 2-2. 배포

1. 대시보드에서 **New +** → **Blueprint**
2. 방금 만든 `dongne-pyeongsaeng` 저장소 선택
3. Render가 `render.yaml`을 읽어 설정을 자동으로 채웁니다
4. **Apply** 클릭

3~5분 기다리면 됩니다. 로그에 이런 줄이 뜨면 성공입니다.

```
MCP 서버 시작 — http://0.0.0.0:10000/mcp
==> Your service is live 🎉
```

### 2-3. 주소 확인

화면 위쪽에 주소가 나옵니다.

```
https://dongne-pyeongsaeng.onrender.com
```

**브라우저로 그 주소를 열어 보십시오.** 아래 같은 안내문이 보이면 정상입니다.

```
동네 평생학습 MCP 서버가 실행 중입니다.
...
    https://dongne-pyeongsaeng.onrender.com/mcp
```

이 화면에 표시된 **`/mcp`로 끝나는 주소**를 복사해 두십시오. 다음 단계에서 씁니다.

---

## ③ Claude 앱에 커넥터로 등록

1. Claude 앱 → **설정(Settings)** → **커넥터(Connectors)**
2. **사용자 지정 커넥터 추가 (Add custom connector)**
3. 입력
   - 이름: `동네 평생학습`
   - URL: `https://dongne-pyeongsaeng.onrender.com/mcp` ← **끝에 `/mcp` 필수**
4. 추가 후 커넥터 목록에서 **켜기**

### 확인

새 대화에서 이렇게 물어보십시오.

```
경주에 지금 신청할 수 있는 평생학습 강좌 알려줘
```

> 첫 질문은 30초쯤 걸릴 수 있습니다. 무료 요금제는 서버가 잠들었다가 깨어나기 때문입니다.
> 두 번째부터는 빠릅니다.

---

## 대구 API 붙이기 (선택)

1. https://www.data.go.kr/data/15061491/openapi.do → 활용신청 (무료·즉시)
2. 마이페이지에서 **일반 인증키(Decoding)** 와 **요청 주소** 복사
3. Render 대시보드 → 해당 서비스 → **Environment** 탭
4. 두 값을 채우고 저장 → 자동 재배포

```
DAEGU_API_KEY  = 일반 인증키 (Decoding)
DAEGU_API_URL  = 요청 주소
```

---

## 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| Render 배포 실패 | 로그에서 빨간 줄 확인. `requirements.txt`가 저장소에 있는지 |
| 브라우저로 열면 오류 | 배포가 아직 진행 중일 수 있음. 3분 더 기다려 보기 |
| 커넥터 추가가 안 됨 | 주소 끝에 `/mcp`를 붙였는지. `https`인지 |
| 툴은 보이는데 결과가 안 나옴 | Claude에게 "데이터 소스 점검해줘"라고 요청 (`check_sources`) |
| 응답이 매우 느림 | 무료 요금제 특성. 유료($7/월)로 올리면 항상 깨어 있음 |

---

## 알아 두실 것

**이 서버는 누구나 접속할 수 있습니다.** 커스텀 커넥터 주소를 아는 사람은 이용할 수 있다는 뜻입니다.

다만 다루는 정보가 **공개된 평생학습 강좌 안내뿐**이고, 쓰기 기능이 없으며(조회 전용), 개인정보를 저장하지 않습니다. 파일럿 단계에서는 문제되지 않습니다.

주민에게 정식 공개하는 단계로 간다면 이 세 가지를 검토하십시오.

1. **접근 제한** — OAuth 인증을 붙이거나, 주소를 공개하지 않고 필요한 사람에게만 전달
2. **기관 서버 부담** — 이용자가 늘면 경주시 서버에 가는 요청도 늘어납니다. 캐시 시간(`DPS_CACHE_TTL`)을 늘리거나, 하루 한 번 수집해 저장하는 방식으로 바꾸는 편이 낫습니다
3. **경주시 협의** — 공개 서비스로 운영한다면 데이터 출처 기관과 사전에 이야기해 두는 것이 안전합니다. 파일럿·내부 검토 단계라면 필요 없습니다
